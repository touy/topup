var nano = require('nano');
var prom = require('./src');

var db = prom(nano('http://localhost:5984')).db.use('test')

db.insert({'crazy': true }, 'rabbit')
  .then(function(res) {
    var body = res[0]
    var headers = res[1]
    console.log(body)
  })
  .catch(function(err) {
    console.error(err);
  })
